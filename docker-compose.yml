services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: seasonal_backend_prod
    restart: always
    ports:
      - '3000:3000'
    environment:
      DATABASE_URL: ${DATABASE_URL}
